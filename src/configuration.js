define(function(){

	// Default rules to follow (global for now)
	var rules = {
		maxLength: 6,
	  	minLength: 1,
	  	minPasswordLength: 8,
	  	minPasswordStrength: 3 // Out of 0 to 4
	}

	return rules;

});